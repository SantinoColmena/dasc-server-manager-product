# ============================================
# DASC Server Manager - Perfil SINGLE / Lite
# Instalación mínima en un único servidor
# ============================================

# Perfil de instalación
INSTALL_MODE=single
DASC_PROFILE_NAME=DASC Lite
DASC_PROFILE_DESCRIPTION=Panel, base de datos, logs y backups en un único servidor

# Usuario SSH
SSH_USER=dasc

# Hosts
SERVICIOS_HOST=127.0.0.1
BACKUPS_HOST=127.0.0.1
LOGS_DB_HOST=127.0.0.1
DB_HOST=127.0.0.1

# Base de datos principal
DB_NAME=employees
DB_BACKUP_USER=dasc_backup
DB_BACKUP_PASS=cambiar_password_backup

# Logs
LOGS_DB_NAME=dasc_logs
LOGS_DB_USER=dasc_logs
LOGS_DB_PASS=cambiar_password_logs
LOGS_ORIGIN=dasc-web

# Panel
SECRET_KEY=cambiar_clave_secreta
ADMIN_USER=admin
ADMIN_PASSWORD=cambiar_password_admin

# Cacti / monitorización
CACTI_URL=/cacti/

# Copia externa obligatoria en modo Lite
EXTERNAL_BACKUP_REQUIRED=yes
EXTERNAL_BACKUP_ENABLED=no
EXTERNAL_BACKUP_TYPE=none
EXTERNAL_BACKUP_PATH=/mnt/dasc-external

# SFTP/NAS futuro
EXTERNAL_SFTP_HOST=
EXTERNAL_SFTP_PORT=22
EXTERNAL_SFTP_USER=
EXTERNAL_SFTP_REMOTE_PATH=

# Cifrado futuro
EXTERNAL_BACKUP_ENCRYPTION=none
EXTERNAL_SYNC_DELETE=no
EXTERNAL_GPG_PASSPHRASE=
