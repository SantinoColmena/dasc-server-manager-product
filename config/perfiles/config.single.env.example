# ============================================
# Vigex - Perfil SINGLE / Lite
# Instalación mínima en un único servidor
# ============================================

# Perfil de instalación
INSTALL_MODE=single
VIGEX_PROFILE_NAME=Vigex Lite
VIGEX_PROFILE_DESCRIPTION=Panel, base de datos, logs y backups en un único servidor

# Usuario SSH
SSH_USER=vigex

# Hosts
SERVICIOS_HOST=127.0.0.1
BACKUPS_HOST=127.0.0.1
LOGS_DB_HOST=127.0.0.1
DB_HOST=127.0.0.1

# Base de datos principal
DB_NAME=employees
DB_BACKUP_USER=vigex_backup
DB_BACKUP_PASS=cambiar_password_backup

# Logs
LOGS_DB_NAME=vigex_logs
LOGS_DB_USER=vigex_logs
LOGS_DB_PASS=cambiar_password_logs
LOGS_ORIGIN=vigex-web

# Panel
SECRET_KEY=cambiar_clave_secreta
ADMIN_USER=admin
ADMIN_PASSWORD=cambiar_password_admin

# Cacti / monitorización
CACTI_URL=/cacti/

# Copia externa obligatoria en modo Lite
EXTERNAL_BACKUP_REQUIRED=yes
EXTERNAL_BACKUP_ENABLED=no
EXTERNAL_BACKUP_TYPE=none
EXTERNAL_BACKUP_PATH=/mnt/vigex-external

# SFTP/NAS futuro
EXTERNAL_SFTP_HOST=
EXTERNAL_SFTP_PORT=22
EXTERNAL_SFTP_USER=
EXTERNAL_SFTP_REMOTE_PATH=

# Cifrado futuro
EXTERNAL_BACKUP_ENCRYPTION=none
EXTERNAL_SYNC_DELETE=no
EXTERNAL_GPG_PASSPHRASE=

# ── Vigex Agent — servidores Windows gestionados (R-101/R-102) ───────────────
# Puerto en que escucha el agente en cada servidor Windows (por defecto 8050).
VIGEX_AGENT_PORT=8050
# Hosts Windows a gestionar. Formato: "ip1:token1,ip2:token2"
# Dejar vacío si todos los servidores son Linux (el panel usará SSH).
# En perfil Lite es habitual que el servidor único sea Linux; solo rellenar
# si el servidor de backups/servicios es Windows con VigexAgent.exe instalado.
VIGEX_AGENT_TOKEN_MAP=
